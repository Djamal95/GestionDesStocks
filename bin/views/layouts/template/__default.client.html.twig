<!DOCTYPE html>
<html lang="fr">
      <head>
              <meta charset="utf-8">
              <meta http-equiv="X-UA-Compatible" content="IE=edge">
              <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1.0, shrink-to-fit=no">
              <link rel="stylesheet" href="{{ __css('bootstrap/4.1/bootstrap.min') }}">
              <link href="{{ __cssfont('font-awesome-min') }}" rel="stylesheet" type="text/css">
              <link href="{{ __cssbsico('bootstrap-icons') }}" rel="stylesheet" type="text/css">
              <link href="{{ __cssiconfont('icofont.min') }}" rel="stylesheet" type="text/css">
              <link rel="stylesheet" type="text/css" href="{{ __css('default/default.main.style') }}">
              <script src="{{ __js('jquery/jquery.min') }}"></script>
              <script src="{{ __js('jquery/axios.min') }}"></script>
              <link rel="icon" href="{{ __img('logo_o.png') }}">
              <title>{{ __msg('site-title') }}</title>
      </head>
      <body>
              {{ include('layouts/display/__header.html.html') }}

              <!--  ========== Nav bar ================  -->
              <ul class="nav nav-pills">
                    <li class="nav-item">
                      <a class="nav-link active" href="#">Active</a>
                    </li>
                    <li class="nav-item dropdown">
                      <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-expanded="false">Dropdown</a>
                      <div class="dropdown-menu">
                        <a class="dropdown-item" href="#">
                            <i class="bi bi-box-seam"></i>
                            <span>Action</span>
                        </a>
                        <a class="dropdown-item" href="#">
                            <i class="bi bi-box-seam"></i>
                            <span>Another action</span>
                        </a>
                        <a class="dropdown-item" href="#">
                            <i class="bi bi-box-seam"></i>
                            <span>Something else here</span>
                        </a>
                      </div>
                    </li>
                    <li class="nav-item">
                      <a class="nav-link" href="#">
                        <i class="bi bi-box-seam"></i>
                        <span>Link</span>
                      </a>
                    </li>
              </ul>
              <!--  ========== End Nav bar ================  -->

              {% block content %}{% endblock %}
              {{ include('layouts/display/__footer.html.html') }}
              <script src="{{ __js('jquery/jquery') }}"></script>
              <script src="{{ __js('bootstrap/4.1/bootstrap.bundle.min') }}"></script>
              <script src="{{ __js('bootstrap/4.1/bootstrap.min') }}"></script>
      </body>
</html>
